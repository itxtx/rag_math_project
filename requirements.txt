# requirements.txt
python-dotenv
weaviate-client
PyMuPDF
pylatexenc
requests
# Add other libraries like PyMuPDF, pylatexenc, etc., as we implement parsers
# For example:
# langchain
# sentence-transformers